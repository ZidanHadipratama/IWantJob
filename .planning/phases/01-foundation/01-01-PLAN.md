---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/routers/__init__.py
  - backend/app/routers/resume.py
  - backend/app/routers/form.py
  - backend/app/routers/jobs.py
  - backend/app/routers/chat.py
  - backend/app/services/__init__.py
  - backend/app/services/ai_service.py
  - backend/app/services/pdf_service.py
  - backend/app/services/db_service.py
  - backend/app/models/__init__.py
  - backend/app/models/schemas.py
  - backend/app/prompts/tailor_resume.txt
  - backend/app/prompts/fill_form.txt
  - backend/app/prompts/chat_coach.txt
  - backend/requirements.txt
  - backend/Dockerfile
  - backend/pytest.ini
  - backend/tests/__init__.py
  - backend/tests/test_health.py
  - backend/tests/test_cors.py
  - backend/tests/test_stubs.py
  - docker-compose.yml
autonomous: true
requirements:
  - SETUP-01
  - SETUP-04

must_haves:
  truths:
    - "GET /health returns 200 with {status: ok}"
    - "All 7 router stubs return 501 Not Implemented"
    - "CORS allows chrome-extension:// origins"
    - "docker compose up starts the backend and health check responds"
    - "Pydantic v2 schemas use model_config and field_validator (not v1 patterns)"
  artifacts:
    - path: "backend/app/main.py"
      provides: "FastAPI app with CORS and router includes"
      exports: ["app"]
    - path: "backend/app/routers/resume.py"
      provides: "Resume router stub"
      exports: ["router"]
    - path: "backend/app/routers/form.py"
      provides: "Form router stub"
      exports: ["router"]
    - path: "backend/app/routers/jobs.py"
      provides: "Jobs router stub"
      exports: ["router"]
    - path: "backend/app/routers/chat.py"
      provides: "Chat router stub"
      exports: ["router"]
    - path: "backend/app/models/schemas.py"
      provides: "All Pydantic v2 request/response models"
      min_lines: 50
    - path: "backend/Dockerfile"
      provides: "Python 3.11-slim with WeasyPrint system deps"
      contains: "libcairo2"
    - path: "docker-compose.yml"
      provides: "One-command backend startup"
      contains: "uvicorn"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/routers/*.py"
      via: "app.include_router"
      pattern: "include_router"
    - from: "docker-compose.yml"
      to: "backend/Dockerfile"
      via: "build context"
      pattern: "build.*backend"
---

<objective>
Scaffold the FastAPI backend with all router stubs, Pydantic v2 schemas, Docker Compose, and test infrastructure.

Purpose: Establish the backend tier with correct architecture boundaries (routers, services, models) so Phase 2-3 can implement features against defined module structure.
Output: Working Docker container serving health check and 7 stub endpoints, with passing smoke tests.
</objective>

<execution_context>
@/home/ikktaa/.claude/get-shit-done/workflows/execute-plan.md
@/home/ikktaa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FastAPI backend with Docker and all stubs</name>
  <files>
    backend/app/__init__.py,
    backend/app/main.py,
    backend/app/routers/__init__.py,
    backend/app/routers/resume.py,
    backend/app/routers/form.py,
    backend/app/routers/jobs.py,
    backend/app/routers/chat.py,
    backend/app/services/__init__.py,
    backend/app/services/ai_service.py,
    backend/app/services/pdf_service.py,
    backend/app/services/db_service.py,
    backend/app/models/__init__.py,
    backend/app/models/schemas.py,
    backend/app/prompts/tailor_resume.txt,
    backend/app/prompts/fill_form.txt,
    backend/app/prompts/chat_coach.txt,
    backend/requirements.txt,
    backend/Dockerfile,
    docker-compose.yml
  </files>
  <action>
Create the entire backend directory structure per SRS Section 9.

**backend/app/main.py:**
- FastAPI app with title="JobPilot API", version="0.1.0"
- CORSMiddleware with allow_origins=["chrome-extension://*"], allow_credentials=True, allow_methods=["*"], allow_headers=["*"]
- Include all 4 routers (resume, form, jobs, chat)
- GET /health endpoint returning {"status": "ok", "service": "jobpilot-backend"}

**Router stubs (4 files):**
- backend/app/routers/resume.py: router with POST /tailor-resume and POST /generate-pdf -- both raise HTTPException(501, "Not implemented - Phase 3")
- backend/app/routers/form.py: router with POST /fill-form and POST /save-qa -- both raise HTTPException(501)
- backend/app/routers/jobs.py: router with GET /job/{job_id} and POST /log-job -- both raise HTTPException(501)
- backend/app/routers/chat.py: router with POST /chat -- raises HTTPException(501)
- Each router uses APIRouter(tags=["tag_name"])

**Service stubs (3 files):**
- ai_service.py: empty class AIService with pass (placeholder for LiteLLM wrapper)
- pdf_service.py: empty class PDFService with pass (placeholder for WeasyPrint)
- db_service.py: empty class DBService with pass (placeholder for Supabase client)

**Pydantic v2 schemas (backend/app/models/schemas.py):**
Use ONLY v2 API -- model_config = ConfigDict(...), @field_validator, .model_dump(), .model_validate(). Do NOT use class Config, @validator, .dict(), .from_orm().
Define these models per SRS Section 3:
- UserProfile(name, email, work_authorization?, linkedin_url?, github_url?)
- FillFormRequest(form_fields: list[dict], resume_text, user_profile: UserProfile, job_id?: UUID, job_description?: str)
- FillFormResponse(answers: list[dict], job_id: UUID)
- TailorResumeRequest(job_description, resume_text, user_profile: UserProfile)
- TailorResumeResponse(tailored_resume_text, match_score: float, job_id: UUID)
- LogJobRequest(company, title, url?, job_description?, status: str = "saved")
- ChatRequest(message, job_id: UUID)
- ChatResponse(reply, job_id: UUID)
- HealthResponse(status, service)
All models: use ConfigDict(str_strip_whitespace=True).

**Prompt templates (3 .txt files):**
- tailor_resume.txt: placeholder "PROMPT PLACEHOLDER - implement in Phase 3"
- fill_form.txt: same placeholder
- chat_coach.txt: same placeholder

**backend/requirements.txt:**
```
fastapi[standard]>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0
supabase>=2.4.0
weasyprint>=62.0
pdfplumber>=0.11.0
litellm>=1.40.0
```

**backend/Dockerfile:**
```dockerfile
FROM python:3.11-slim
RUN apt-get update && apt-get install -y \
    libcairo2 \
    libpango-1.0-0 \
    libpangocairo-1.0-0 \
    libgdk-pixbuf2.0-0 \
    libffi-dev \
    shared-mime-info \
    && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**docker-compose.yml (project root):**
```yaml
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

All __init__.py files are empty.
  </action>
  <verify>
    <automated>cd /home/ikktaa/app/JobPilot/backend && python -c "from app.main import app; print('FastAPI app imports OK')" && python -c "from app.models.schemas import UserProfile, FillFormRequest, TailorResumeRequest; print('Schemas import OK')"</automated>
  </verify>
  <done>FastAPI app with health check, 7 stub endpoints across 4 routers, 3 service stubs, Pydantic v2 schemas, Dockerfile with WeasyPrint deps, and docker-compose.yml all exist and import without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test infrastructure and smoke tests</name>
  <files>
    backend/pytest.ini,
    backend/tests/__init__.py,
    backend/tests/test_health.py,
    backend/tests/test_cors.py,
    backend/tests/test_stubs.py
  </files>
  <behavior>
    - test_health: GET /health returns 200 with {"status": "ok", "service": "jobpilot-backend"}
    - test_cors: OPTIONS request with Origin: chrome-extension://abc123 gets back access-control-allow-origin header
    - test_stubs_tailor_resume: POST /tailor-resume returns 501
    - test_stubs_generate_pdf: POST /generate-pdf returns 501
    - test_stubs_fill_form: POST /fill-form returns 501
    - test_stubs_save_qa: POST /save-qa returns 501
    - test_stubs_get_job: GET /job/fake-uuid returns 501
    - test_stubs_log_job: POST /log-job returns 501
    - test_stubs_chat: POST /chat returns 501
  </behavior>
  <action>
Create pytest config and smoke tests using httpx AsyncClient with FastAPI's TestClient pattern.

**backend/pytest.ini:**
```ini
[pytest]
testpaths = tests
asyncio_mode = auto
```

**backend/tests/__init__.py:** Empty file.

**backend/tests/test_health.py:**
Use `from httpx import AsyncClient, ASGITransport` and `from app.main import app`. Create async test that GETs /health and asserts 200 status + correct JSON body.

**backend/tests/test_cors.py:**
Send an OPTIONS request to /health with headers {"Origin": "chrome-extension://abc123def", "Access-Control-Request-Method": "GET"}. Assert response includes "access-control-allow-origin" header.

**backend/tests/test_stubs.py:**
Test all 7 stub endpoints return 501:
- POST /tailor-resume
- POST /generate-pdf
- POST /fill-form
- POST /save-qa
- GET /job/00000000-0000-0000-0000-000000000000
- POST /log-job
- POST /chat

Each test uses httpx AsyncClient with ASGITransport(app=app). Install dev deps: add pytest, pytest-asyncio, httpx to requirements or test separately with `pip install pytest pytest-asyncio httpx`.
  </action>
  <verify>
    <automated>cd /home/ikktaa/app/JobPilot/backend && pip install pytest pytest-asyncio httpx -q && pytest tests/ -v</automated>
  </verify>
  <done>All 9+ tests pass: health check returns 200, CORS headers present for chrome-extension origin, all 7 stubs return 501.</done>
</task>

</tasks>

<verification>
- `cd backend && pytest tests/ -v` -- all tests green
- `python -c "from app.main import app"` -- no import errors
- `docker compose config` -- validates docker-compose.yml syntax
- Pydantic v2 patterns only (no class Config, no @validator, no .dict())
</verification>

<success_criteria>
1. All smoke tests pass (health, CORS, 7 stubs)
2. FastAPI app imports cleanly with all routers wired
3. Pydantic v2 schemas defined with correct v2 API
4. Dockerfile includes WeasyPrint system dependencies
5. docker-compose.yml enables hot reload via volume mount
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/001_initial_schema.sql
  - supabase/migrations/002_form_qa_pairs.sql
autonomous: false
requirements:
  - SETUP-02

must_haves:
  truths:
    - "All 5 tables exist: users, jobs, form_qa_pairs, resumes, chat_messages"
    - "RLS is explicitly enabled on every table"
    - "RLS policies restrict access to user_id = auth.uid()"
    - "Foreign keys cascade correctly (job delete cascades Q&A and chat)"
    - "form_qa_pairs has UNIQUE(job_id, field_id) constraint"
  artifacts:
    - path: "supabase/migrations/001_initial_schema.sql"
      provides: "users, jobs, resumes, chat_messages tables + RLS"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "supabase/migrations/002_form_qa_pairs.sql"
      provides: "form_qa_pairs table + RLS"
      contains: "UNIQUE(job_id, field_id)"
  key_links:
    - from: "jobs.user_id"
      to: "users.id"
      via: "REFERENCES ON DELETE CASCADE"
      pattern: "REFERENCES users.*ON DELETE CASCADE"
    - from: "form_qa_pairs.job_id"
      to: "jobs.id"
      via: "REFERENCES ON DELETE CASCADE"
      pattern: "REFERENCES jobs.*ON DELETE CASCADE"
---

<objective>
Create Supabase SQL migration files for all 5 database tables with RLS policies.

Purpose: Establish the data layer so Phase 2 can build the DB service layer and CRUD operations against real tables.
Output: Two migration SQL files ready to apply via Supabase Dashboard SQL editor or CLI.
</objective>

<execution_context>
@/home/ikktaa/.claude/get-shit-done/workflows/execute-plan.md
@/home/ikktaa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL migration files for all 5 tables with RLS</name>
  <files>
    supabase/migrations/001_initial_schema.sql,
    supabase/migrations/002_form_qa_pairs.sql
  </files>
  <action>
Create two SQL migration files exactly matching the research-verified schema from 01-RESEARCH.md.

**supabase/migrations/001_initial_schema.sql:**

Create 4 tables in this order (dependency order matters):

1. **users** table:
   - id: UUID PRIMARY KEY DEFAULT gen_random_uuid()
   - name: TEXT
   - email: TEXT
   - linkedin_url: TEXT
   - github_url: TEXT
   - work_authorization: TEXT
   - base_resume_text: TEXT
   - created_at: TIMESTAMPTZ DEFAULT now()

2. **jobs** table:
   - id: UUID PRIMARY KEY DEFAULT gen_random_uuid()
   - user_id: UUID REFERENCES users(id) ON DELETE CASCADE
   - company: TEXT
   - title: TEXT
   - url: TEXT
   - job_description: TEXT
   - status: TEXT DEFAULT 'saved'
   - applied_at: TIMESTAMPTZ
   - notes: TEXT
   - created_at: TIMESTAMPTZ DEFAULT now()

3. **resumes** table:
   - id: UUID PRIMARY KEY DEFAULT gen_random_uuid()
   - user_id: UUID REFERENCES users(id) ON DELETE CASCADE
   - job_id: UUID REFERENCES jobs(id) ON DELETE SET NULL
   - resume_text: TEXT
   - pdf_url: TEXT
   - is_base: BOOLEAN DEFAULT false
   - created_at: TIMESTAMPTZ DEFAULT now()

4. **chat_messages** table:
   - id: UUID PRIMARY KEY DEFAULT gen_random_uuid()
   - job_id: UUID REFERENCES jobs(id) ON DELETE CASCADE
   - user_id: UUID REFERENCES users(id) ON DELETE CASCADE
   - role: TEXT
   - content: TEXT
   - created_at: TIMESTAMPTZ DEFAULT now()

After all 4 tables, add:
- ALTER TABLE ... ENABLE ROW LEVEL SECURITY for each table (CRITICAL -- must come before policies)
- CREATE POLICY for each table: FOR ALL USING (auth.uid() = id) for users, FOR ALL USING (auth.uid() = user_id) for the rest

**supabase/migrations/002_form_qa_pairs.sql:**

1. **form_qa_pairs** table:
   - id: UUID PRIMARY KEY DEFAULT gen_random_uuid()
   - job_id: UUID REFERENCES jobs(id) ON DELETE CASCADE
   - user_id: UUID REFERENCES users(id) ON DELETE CASCADE
   - field_id: TEXT
   - question: TEXT
   - answer: TEXT
   - field_type: TEXT
   - edited_by_user: BOOLEAN DEFAULT false
   - created_at: TIMESTAMPTZ DEFAULT now()
   - updated_at: TIMESTAMPTZ DEFAULT now()
   - UNIQUE(job_id, field_id)

2. Enable RLS and create policy.

Add a comment header to each file with the migration purpose.
  </action>
  <verify>
    <automated>cd /home/ikktaa/app/JobPilot && grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/001_initial_schema.sql supabase/migrations/002_form_qa_pairs.sql && grep -c "CREATE POLICY" supabase/migrations/001_initial_schema.sql supabase/migrations/002_form_qa_pairs.sql && grep "UNIQUE" supabase/migrations/002_form_qa_pairs.sql</automated>
  </verify>
  <done>Two migration files exist with all 5 tables, RLS enabled on every table, cascade deletes configured, and UNIQUE(job_id, field_id) on form_qa_pairs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify database tables in Supabase Dashboard</name>
  <files>supabase/migrations/001_initial_schema.sql, supabase/migrations/002_form_qa_pairs.sql</files>
  <action>
User applies the migration SQL files to their Supabase instance and verifies tables and RLS are configured correctly.

What was built: SQL migration files for all 5 Supabase tables with RLS policies.

How to verify:
1. Open your Supabase Dashboard and go to SQL Editor
2. Copy and paste the contents of supabase/migrations/001_initial_schema.sql and run it
3. Copy and paste the contents of supabase/migrations/002_form_qa_pairs.sql and run it
4. Go to Table Editor -- verify all 5 tables exist: users, jobs, form_qa_pairs, resumes, chat_messages
5. Go to Auth and then Policies -- verify each table shows "RLS Enabled" badge and has a policy
6. Alternative: If using Supabase CLI, run `supabase db push` from the project root

Resume signal: Type "approved" if tables and RLS are confirmed, or describe any SQL errors.
  </action>
  <verify>User confirms tables exist and RLS is enabled in Supabase Dashboard</verify>
  <done>All 5 tables created in Supabase with RLS enabled and policies active.</done>
</task>

</tasks>

<verification>
- All 5 tables defined with correct column types and constraints
- RLS enabled on every table (grep for "ENABLE ROW LEVEL SECURITY" returns 5 matches total)
- Cascade deletes on jobs->form_qa_pairs, jobs->chat_messages
- UNIQUE constraint on form_qa_pairs(job_id, field_id)
- Human confirms tables exist in Supabase Dashboard
</verification>

<success_criteria>
1. Migration files are syntactically valid SQL
2. All 5 tables created in Supabase
3. RLS enabled and policies active on every table
4. Foreign key cascades work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
